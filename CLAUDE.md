# Course Platform - Agent Instructions

Educational content delivery for technical courses.

## Tech Stack
- Python 3.13+ / FastAPI
- React + TypeScript + Vite
- PostgreSQL + Redis
- Cloudflare Stream for video

## Commands
```bash
# Backend
cd backend && uv sync && uv run uvicorn main:app --reload

# Frontend
cd frontend && npm install && npm run dev
```
